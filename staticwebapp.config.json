{
  "routes": [
    {
      "route": "/admin",
      "rewrite": "/admin.html"
    },
    {
      "route": "/login",
      "rewrite": "/login.html"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/icons/*", "/*.{css,js,json,svg,png,jpg,jpeg,webp}"]
  }
}
